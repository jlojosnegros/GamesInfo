apiVersion: v1
kind: Service
metadata:
  name: {{ include "gamesInfo.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "gamesInfo.name" . }}
    helm.sh/chart: {{ include "gamesInfo.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "gamesInfo.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
